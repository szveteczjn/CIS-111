var width = 8.5 ;

var length = 11 ;

var diagonal = Math.round(Math.sqrt(Math.pow(length, 2) + Math.pow(width, 2)));

var perimeter = width * 2 + length * 2

console.log("Diagonal=" + diagonal + "\n"
				+ "Perimeter=" + perimeter + "\n");

alert("Diagonal=" + diagonal + "\n"
				+ "Perimeter=" + perimeter + "\n");